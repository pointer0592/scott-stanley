import {sds_background_urls } from '../../themes/tailwindcss/tokens.mjs'

const heroImageUrl =
    sds_background_urls['design-image-large'] || sds_background_urls['design-image']

export { heroImageUrl }
